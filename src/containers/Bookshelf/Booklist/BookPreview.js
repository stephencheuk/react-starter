import BookPreviewFrame from "./BookPreview_iframe";
// import BookPreviewFrame from "./BookPreview_embed";

const BookPreview = (props) => {

    return (
        <BookPreviewFrame {...props} />
    )
}

export default BookPreview;
